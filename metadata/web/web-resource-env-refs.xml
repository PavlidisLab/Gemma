<!-- 
	Reference to an external resource.  We are not using this because our references are in the 
	Spring context (uncomment if not using Spring).
	
	$Id$ 
	
-->
<!-- 
    <resource-ref>
        <description>DB Connection</description>
        <res-ref-name>jdbc/gemd</res-ref-name>
        <res-type>javax.sql.DataSource</res-type>
        <res-auth>Container</res-auth>
    </resource-ref>
-->    
    