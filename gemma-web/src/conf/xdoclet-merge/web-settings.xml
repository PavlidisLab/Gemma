<display-name>Gemma</display-name>
<context-param>
    <param-name>listenPort_http</param-name>
    <param-value>8080</param-value>
</context-param>
<context-param>
    <param-name>listenPort_https</param-name>
    <param-value>8443</param-value>
</context-param>

<!-- Possible values: hibernate -->
<context-param>
    <param-name>daoType</param-name>
    <param-value>hibernate</param-value>
</context-param>

<!-- Define the basename for a resource bundle for I18N -->
<context-param>
    <param-name>javax.servlet.jsp.jstl.fmt.localizationContext</param-name>
    <param-value>messages</param-value>
</context-param>

<!-- Fallback locale if no bundles found for browser's preferred locale -->
<!-- Force a single locale using param-name 'javax.servlet.jsp.jstl.fmt.locale' -->
<context-param>
    <param-name>javax.servlet.jsp.jstl.fmt.fallbackLocale</param-name>
    <param-value>en</param-value>
</context-param>

<!-- Name of Log4j config file relative to webapp root directory.  This is 
    done by the org.springframework.web.util.Log4jWebConfigurer -->
<context-param>
    <param-name>log4jConfigLocation</param-name>
    <param-value>/WEB-INF/classes/log4j.properties</param-value>
</context-param>

<!-- Context Configuration locations for Spring XML files -->
<context-param>
    <param-name>contextConfigLocation</param-name>
    <param-value>
        classpath*:/ubic/gemma/applicationContext-*.xml, classpath*:/ubic/gemma/beanRefFactory.xml,
        classpath*:/ubic/gemma/localDataSource.xml
    </param-value>
</context-param>

<!-- Add the bootstrap listener to start the root WebApplicationContext -->
<!-- cannot use with spring-1.2.8.jar (used with spring-1.2.6.jar).
<listener>
    <listener-class>org.springframework.web.context.ContextLoaderListener</listener-class>
</listener>
-->
