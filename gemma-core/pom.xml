<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <parent>
        <artifactId>gemma</artifactId>
        <groupId>gemma</groupId>
        <version>1.32.7-SNAPSHOT</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>
    <artifactId>gemma-core</artifactId>
    <name>Gemma Core</name>
    <description>Module containing Gemma core features</description>
    <build>
        <plugins>
            <plugin>
                <groupId>io.github.git-commit-id</groupId>
                <artifactId>git-commit-id-maven-plugin</artifactId>
                <!-- 9.2.0 is the latest 9.x. The 10.x line requires Maven 3.9+, but
                     Jenkins still pins Maven 3.6.3 (see .jenkins/Jenkinsfile), so 10.x
                     is deferred until the CI toolchain is bumped. Revisit then; the only
                     real 9 -> 10 config change is that <includeOnlyProperties> moved from
                     exact-match to regex (the regex form is
                     <includeOnlyProperty>^git\.commit\.id$</includeOnlyProperty>). -->
                <version>9.2.0</version>
                <executions>
                    <execution>
                        <id>generate-build-info</id>
                        <phase>initialize</phase>
                        <goals>
                            <goal>revision</goal>
                        </goals>
                        <configuration>
                            <offline>true</offline>
                            <useNativeGit>true</useNativeGit>
                            <includeOnlyProperties>git.commit.id</includeOnlyProperties>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <executions>
                    <execution>
                        <id>default-jar</id>
                        <goals>
                            <goal>jar</goal>
                        </goals>
                        <configuration>
                            <archive>
                                <manifestEntries>
                                    <Gemma-Version>${project.version}</Gemma-Version>
                                    <Gemma-Build-Timestamp>${maven.build.timestamp}</Gemma-Build-Timestamp>
                                    <!--suppress MavenModelInspection -->
                                    <Gemma-Build-GitHash>${git.commit.id}</Gemma-Build-GitHash>
                                </manifestEntries>
                            </archive>
                        </configuration>
                    </execution>
                    <execution>
                        <id>package-test-jar</id>
                        <goals>
                            <goal>test-jar</goal>
                        </goals>
                        <configuration>
                            <!-- only include test classes that are meant to be re-used -->
                            <includes>
                                <include>org/springframework/security/test/**/*.class</include>
                                <include>ubic/gemma/core/ontology/TestOntologyConfig.class</include>
                                <include>ubic/gemma/core/util/test/**/*.class</include>
                                <!-- FIXME: this is not a scalable approach, we should have a separate test module -->
                                <include>ubic/gemma/persistence/service/expression/experiment/RandomExpressionExperimentUtils.class</include>
                                <include>ubic/gemma/persistence/service/expression/experiment/RandomExperimentalDesignUtils.class</include>
                                <include>ubic/gemma/persistence/service/expression/bioAssayData/NegativeBinomialDistribution.class</include>
                                <include>ubic/gemma/persistence/service/expression/bioAssayData/RandomBulkDataUtils.class</include>
                                <include>ubic/gemma/persistence/service/expression/bioAssayData/RandomDataUtils.class</include>
                                <include>ubic/gemma/persistence/service/expression/bioAssayData/RandomExpressionDataMatrixUtils.class</include>
                                <include>ubic/gemma/persistence/service/expression/bioAssayData/RandomSingleCellDataUtils.class</include>
                                <include>ubic/gemma/applicationContext-dataSourceInitializer.xml</include>
                                <include>log4j2-test.xml</include>
                            </includes>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <!-- Phase 2: pre-Spring-6 this unpacked spring-security-test 4.0.4 sources and recompiled
                 them under our own classpath (the 4.x build excluded the MockMvc setup helpers). The 4.0
                 sources reference javax.servlet which doesn't exist in Spring 6, so we now consume
                 spring-security-test as a regular test-jar dependency. -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>build-helper-maven-plugin</artifactId>
                <version>3.6.1</version>
            </plugin>
        </plugins>
    </build>
    <dependencies>
        <!-- Spring -->
        <!-- this is the latest for Spring 3 (>= 1.1 requires Spring 4) -->
        <dependency>
            <groupId>org.springframework.retry</groupId>
            <artifactId>spring-retry</artifactId>
            <version>1.0.3.RELEASE</version>
            <exclusions>
                <exclusion>
                    <groupId>org.springframework</groupId>
                    <artifactId>spring-context</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <!-- Email -->
        <dependency>
            <groupId>com.sun.mail</groupId>
            <artifactId>javax.mail</artifactId>
            <version>1.6.2</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.apache.velocity</groupId>
            <artifactId>velocity-engine-core</artifactId>
            <version>2.4.1</version>
            <exclusions>
                <exclusion>
                    <groupId>org.apache.commons</groupId>
                    <artifactId>commons-lang3</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <!-- Renovations Phase 2: Lucene + Hibernate Search removed. SearchService is stubbed to return
             empty results; reinstating real full-text search is future work (Hibernate Search 7 +
             Lucene 9, or external OpenSearch/Postgres FTS). -->

        <!-- Apache Commons -->
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-csv</artifactId>
            <version>1.14.1</version>
            <exclusions>
                <exclusion>
                    <groupId>commons-io</groupId>
                    <artifactId>commons-io</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>commons-codec</groupId>
                    <artifactId>commons-codec</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-math3</artifactId>
            <version>3.6.1</version>
        </dependency>
        <dependency>
            <groupId>commons-net</groupId>
            <artifactId>commons-net</artifactId>
            <version>3.12.0</version>
            <exclusions>
                <exclusion>
                    <groupId>commons-io</groupId>
                    <artifactId>commons-io</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <!-- Swagger (only annotations, implementation is reserved to Gemma REST) -->
        <dependency>
            <groupId>io.swagger.core.v3</groupId>
            <artifactId>swagger-annotations</artifactId>
            <version>${swagger.version}</version>
        </dependency>

        <!-- Utilities -->
        <!-- JAXB API: jakarta (4.x) namespace. Phase 2 moved off javax.xml.bind:jaxb-api:2.3.1. -->
        <dependency>
            <groupId>jakarta.xml.bind</groupId>
            <artifactId>jakarta.xml.bind-api</artifactId>
            <version>4.0.5</version>
        </dependency>
        <dependency>
            <groupId>obo</groupId>
            <artifactId>org.geneontology</artifactId>
            <version>1.002</version>
        </dependency>
        <!-- bit array -->
        <!-- it can't be updated though because the binary format has changed, and we store those in the database -->
        <dependency>
            <groupId>com.googlecode.javaewah</groupId>
            <artifactId>JavaEWAH</artifactId>
            <!--suppress MavenPackageUpdate -->
            <version>0.7.9</version>
        </dependency>

        <!-- MTJ, for reading MatrixMarket formats -->
        <!-- this is also declared in baseCode, but we use it explicitly for reading single-cell data -->
        <dependency>
            <groupId>com.googlecode.matrix-toolkits-java</groupId>
            <artifactId>mtj</artifactId>
            <version>1.0.4</version>
            <exclusions>
                <exclusion>
                    <groupId>com.github.fommil.netlib</groupId>
                    <artifactId>core</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>com.github.fommil.netlib</groupId>
            <artifactId>core</artifactId>
            <version>1.1.2</version>
            <scope>runtime</scope>
        </dependency>

        <!-- HDF5 -->
        <dependency>
            <groupId>org.hdf5group</groupId>
            <artifactId>hdf5</artifactId>
            <version>${hdf5.version}</version>
        </dependency>

        <!-- Gemma Slack Bot -->
        <dependency>
            <groupId>com.slack.api</groupId>
            <artifactId>slack-api-client</artifactId>
            <version>${slack.version}</version>
            <exclusions>
                <exclusion>
                    <groupId>org.jetbrains.kotlin</groupId>
                    <artifactId>kotlin-stdlib-jdk8</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>com.slack.api</groupId>
            <artifactId>slack-api-model</artifactId>
            <version>${slack.version}</version>
        </dependency>
        <dependency>
            <groupId>org.jetbrains.kotlin</groupId>
            <artifactId>kotlin-stdlib-jdk8</artifactId>
            <version>1.9.25</version>
        </dependency>

        <!-- Scheduling -->
        <!-- This is the latest Quartz release compatible with Spring 3 -->
        <dependency>
            <groupId>org.quartz-scheduler</groupId>
            <artifactId>quartz</artifactId>
            <version>1.8.6</version>
        </dependency>

        <!-- jakarta.servlet-api + spring-security-web: previously test-scoped (only spring-security-test
             needed them). Phase 3 gsec absorption: gemma.gsec.authentication.Ajax*Handler (formerly
             provided by the gemma-gsec JAR at compile time) now lives under gemma-core/src/main/java
             and references HttpServletRequest/Response + SavedRequestAwareAuthenticationSuccessHandler,
             so these deps are promoted to compile scope. servlet-api stays `provided` since the
             container (Tomcat/Jetty) supplies the implementation at runtime. -->
        <dependency>
            <groupId>jakarta.servlet</groupId>
            <artifactId>jakarta.servlet-api</artifactId>
            <version>6.0.0</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-web</artifactId>
            <version>${spring.security.version}</version>
        </dependency>
        <!-- Phase 3 gsec absorption: org.json.JSONObject used by Ajax*Handler is inherited
             from the root pom's <dependencies> block (org.json:json:20240303), no redeclaration needed. -->

        <!-- Testing -->
        <!-- Phase 2: pull spring-security-test as a regular jar rather than unpacking 4.0 sources. -->
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <version>${spring.security.version}</version>
            <scope>test</scope>
        </dependency>

        <!-- Phase 3 first-wave: Flyway-managed schema versioning. Scope is `test` for now; the
             H2 path (BaseDatabaseTest) is the only consumer of these migrations. Production
             MySQL wiring is the follow-on session (see FLYWAY_PROD_FOLLOWUP.md) and will pull
             flyway-core + flyway-mysql into runtime scope. H2 is supported directly by
             flyway-core (no separate flyway-database-h2 module exists). -->
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
            <version>${flyway.version}</version>
            <scope>test</scope>
        </dependency>

        <!-- Phase 3 logging A: JsonTemplateLayout for the production rolling-file appender
             (gemma-web + gemma-cli log4j2.xml). Version is managed by the root pom's
             <dependencyManagement> at ${log4j.version}. Runtime scope: the layout is loaded
             reflectively by log4j-core during config parsing, no compile-time references. -->
        <dependency>
            <groupId>org.apache.logging.log4j</groupId>
            <artifactId>log4j-layout-template-json</artifactId>
            <scope>runtime</scope>
        </dependency>

    </dependencies>

</project>
