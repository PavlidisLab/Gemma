<?xml version="1.0" encoding="UTF-8"?>

<beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:aop="http://www.springframework.org/schema/aop"
	   xmlns:tx="http://www.springframework.org/schema/tx"
	   xmlns="http://www.springframework.org/schema/beans" xmlns:cache="http://www.springframework.org/schema/cache"
	   xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
   http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx.xsd
   http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop.xsd http://www.springframework.org/schema/cache http://www.springframework.org/schema/cache/spring-cache.xsd">

	<!--
		Renovations Phase 2 wiring: Spring 6 + Hibernate 6 (jakarta).

		Spring 6's org.springframework.orm.hibernate5 package was kept for source compatibility
		with Hibernate 5 only — its LocalSessionFactoryBean/HibernateTransactionManager call into
		hibernate-commons-annotations ReflectionManager.reset(), which no longer exists in
		Hibernate 6. The path forward is JPA: build an EntityManagerFactory via Spring's
		LocalContainerEntityManagerFactoryBean (consuming META-INF/persistence.xml) and expose a
		Hibernate SessionFactory by unwrapping it, so existing SessionFactory-injected DAOs
		continue to work without porting every DAO to JPA APIs.
	-->

	<!-- Enable AspectJ autoproxy -->
	<aop:aspectj-autoproxy/>

	<!-- Automatically retry certain failed operations (marked with @Transactional or explicitly with @Retry) -->
	<aop:config>
		<aop:pointcut id="retryable"
					  expression="ubic.gemma.persistence.util.Pointcuts.retryableOrTransactionalServiceMethod()"/>
		<aop:advisor pointcut-ref="retryable" advice-ref="retryAdvice" order="2"/>
	</aop:config>

	<bean id="retryAdvice" class="org.springframework.retry.interceptor.RetryOperationsInterceptor">
		<property name="retryOperations">
			<bean class="org.springframework.retry.support.RetryTemplate">
				<property name="backOffPolicy">
					<bean class="org.springframework.retry.backoff.ExponentialBackOffPolicy"/>
				</property>
				<property name="retryPolicy" ref="retryPolicy">
				</property>
				<property name="listeners">
					<list>
						<ref bean="retryLogger"/>
					</list>
				</property>
			</bean>
		</property>
	</bean>

	<bean id="retryPolicy" class="org.springframework.retry.policy.SimpleRetryPolicy">
		<constructor-arg name="maxAttempts" value="${gemma.transaction.maxretries}"/>
		<constructor-arg name="retryableExceptions">
			<map>
				<entry key="org.hibernate.StaleObjectStateException" value="true"/>
				<entry key="org.hibernate.StaleStateException" value="true"/>
			</map>
		</constructor-arg>
		<constructor-arg name="traverseCauses" value="true"/>
	</bean>

	<!-- JPA EntityManagerFactory, configured from META-INF/persistence.xml ("gemma" unit). -->
	<bean id="entityManagerFactory" class="org.springframework.orm.jpa.LocalContainerEntityManagerFactoryBean"
		  depends-on="dataSourceInitializer">
		<property name="dataSource" ref="dataSource"/>
		<property name="persistenceUnitName" value="gemma"/>
		<property name="jpaVendorAdapter">
			<bean class="org.springframework.orm.jpa.vendor.HibernateJpaVendorAdapter"/>
		</property>
		<property name="jpaPropertyMap">
			<map>
				<entry key="hibernate.hbm2ddl.auto" value="${gemma.hibernate.hbm2ddl.auto}"/>
				<entry key="hibernate.dialect" value="ubic.gemma.persistence.hibernate.MySQL57InnoDBDialect"/>
				<!-- L2 cache: JCache (Ehcache 3 jakarta). EhCache 2 was retired in Phase 2 Step 5b. -->
				<entry key="hibernate.cache.region.factory_class" value="jcache"/>
				<entry key="hibernate.javax.cache.provider" value="org.ehcache.jsr107.EhcacheCachingProvider"/>
				<entry key="hibernate.javax.cache.missing_cache_strategy" value="create"/>
				<entry key="hibernate.cache.use_query_cache" value="true"/>
				<entry key="hibernate.cache.use_second_level_cache" value="true"/>
				<!-- defaults for fetching/inserting -->
				<entry key="hibernate.max_fetch_depth" value="3"/>
				<entry key="hibernate.default_batch_fetch_size" value="${gemma.hibernate.default_batch_fetch_size}"/>
				<entry key="hibernate.jdbc.batch_size" value="${gemma.hibernate.jdbc_batch_size}"/>
				<entry key="hibernate.jdbc.batch_versioned_data" value="true"/>
				<entry key="hibernate.order_inserts" value="true"/>
				<entry key="hibernate.order_updates" value="true"/>
				<!-- used for micrometer -->
				<entry key="hibernate.generate_statistics" value="true"/>
				<!-- debugging options -->
				<entry key="hibernate.show_sql" value="${gemma.hibernate.show_sql}"/>
				<entry key="hibernate.format_sql" value="${gemma.hibernate.format_sql}"/>
			</map>
		</property>
	</bean>

	<!-- Hibernate SessionFactory unwrapped from the JPA EntityManagerFactory so SessionFactory-injected
		 DAOs (most of Gemma's persistence layer) continue to work. -->
	<bean id="sessionFactory" factory-bean="entityManagerFactory" factory-method="unwrap">
		<constructor-arg value="org.hibernate.SessionFactory"/>
	</bean>

	<bean id="transactionManager" class="org.springframework.orm.jpa.JpaTransactionManager">
		<property name="entityManagerFactory" ref="entityManagerFactory"/>
	</bean>

	<tx:annotation-driven order="3"/>

	<!-- enable usage of @Cacheable annotations -->
	<cache:annotation-driven order="2"/>

	<!-- Spring CacheManager for @Cacheable / consumers that inject CacheManager. The "ehcache"
		 bean (a ConcurrentMapCacheManager) is provided by EhcacheConfig; alias it to "cacheManager"
		 so legacy injection points keep working. Proper EhCache 3 / Caffeine integration is a
		 follow-up phase. -->
	<alias name="ehcache" alias="cacheManager"/>
</beans>
