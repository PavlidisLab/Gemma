<?xml version="1.0" encoding="UTF-8"?>

<beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:aop="http://www.springframework.org/schema/aop"
	   xmlns:tx="http://www.springframework.org/schema/tx"
	   xmlns="http://www.springframework.org/schema/beans" xmlns:cache="http://www.springframework.org/schema/cache"
	   xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
   http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx.xsd
   http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop.xsd http://www.springframework.org/schema/cache http://www.springframework.org/schema/cache/spring-cache.xsd">

	<!--
		Renovations Phase 2 wiring: Spring 6 + Hibernate 6.

		Bootstrap path: native Hibernate Configuration via HibernateSessionFactoryBean (not JPA).
		We tried the JPA-bootstrap approach (LocalContainerEntityManagerFactoryBean +
		HibernateJpaVendorAdapter + factory-method="unwrap") but Hibernate's JPA bootstrap
		hard-codes hibernate.current_session_context_class=jpa which breaks
		SessionFactory.getCurrentSession() through Spring's SpringSessionContext bridge to
		HibernateTransactionManager. Gemma's DAOs use getCurrentSession() throughout, so we go
		native — HibernateSessionFactoryBean accepts the standard Hibernate properties and
		honours the SpringSessionContext setting.
	-->

	<!-- Enable AspectJ autoproxy -->
	<aop:aspectj-autoproxy/>

	<!-- Automatically retry certain failed operations (marked with @Transactional or explicitly with @Retry) -->
	<aop:config>
		<aop:pointcut id="retryable"
					  expression="ubic.gemma.persistence.util.Pointcuts.retryableOrTransactionalServiceMethod()"/>
		<aop:advisor pointcut-ref="retryable" advice-ref="retryAdvice" order="2"/>
	</aop:config>

	<bean id="retryAdvice" class="org.springframework.retry.interceptor.RetryOperationsInterceptor">
		<property name="retryOperations">
			<bean class="org.springframework.retry.support.RetryTemplate">
				<property name="backOffPolicy">
					<bean class="org.springframework.retry.backoff.ExponentialBackOffPolicy"/>
				</property>
				<property name="retryPolicy" ref="retryPolicy">
				</property>
				<property name="listeners">
					<list>
						<ref bean="retryLogger"/>
					</list>
				</property>
			</bean>
		</property>
	</bean>

	<bean id="retryPolicy" class="org.springframework.retry.policy.SimpleRetryPolicy">
		<constructor-arg name="maxAttempts" value="${gemma.transaction.maxretries}"/>
		<constructor-arg name="retryableExceptions">
			<map>
				<entry key="org.hibernate.StaleObjectStateException" value="true"/>
				<entry key="org.hibernate.StaleStateException" value="true"/>
			</map>
		</constructor-arg>
		<constructor-arg name="traverseCauses" value="true"/>
	</bean>

	<bean id="transactionManager" class="org.springframework.orm.hibernate5.HibernateTransactionManager">
		<property name="sessionFactory" ref="sessionFactory"/>
		<!-- Expose the Hibernate session's JDBC connection via DataSourceUtils so that JdbcTemplate
		     operations targeting the same DataSource participate in the current Hibernate-managed
		     transaction. Required by the Phase 2 JDBC ACL stack (see applicationContext-security.xml
		     and ubic.gemma.core.security.acl.GemmaJdbcAclService): ACL INSERT/UPDATE/SELECT must
		     co-commit with the surrounding @Transactional business call, otherwise readAclById right
		     after createAcl can't see the just-inserted row. -->
		<property name="dataSource" ref="dataSource"/>
	</bean>

	<tx:annotation-driven order="3"/>

	<!-- Hibernate SessionFactory, built natively from hibernate.cfg.xml.
		 depends-on createDatabaseInitializer (test) so the empty gemdtest DB exists before Hibernate
		 tries to connect / run hbm2ddl=create. In prod, createDatabaseInitializer is a dummy bean.
		 dataSourceInitializer (test) depends on this bean in turn, so the SQL data init runs AFTER
		 Hibernate has materialized the schema. -->
	<bean id="sessionFactory" class="ubic.gemma.persistence.hibernate.HibernateSessionFactoryBean"
		  depends-on="createDatabaseInitializer">
		<property name="dataSource" ref="dataSource"/>
		<property name="configLocation" value="classpath:hibernate.cfg.xml"/>
		<property name="hibernateProperties">
			<props>
				<prop key="hibernate.hbm2ddl.auto">${gemma.hibernate.hbm2ddl.auto}</prop>
				<prop key="hibernate.dialect">ubic.gemma.persistence.hibernate.MySQL57InnoDBDialect</prop>
				<!-- Bridge sessionFactory.getCurrentSession() to Spring's HibernateTransactionManager. -->
				<prop key="hibernate.current_session_context_class">org.springframework.orm.hibernate5.SpringSessionContext</prop>
				<!-- L2 cache: JCache (Ehcache 3 jakarta). EhCache 2 was retired in Phase 2 Step 5b. -->
				<prop key="hibernate.cache.region.factory_class">jcache</prop>
				<prop key="hibernate.javax.cache.provider">org.ehcache.jsr107.EhcacheCachingProvider</prop>
				<prop key="hibernate.javax.cache.missing_cache_strategy">create</prop>
				<prop key="hibernate.cache.use_query_cache">true</prop>
				<prop key="hibernate.cache.use_second_level_cache">true</prop>
				<!-- defaults for fetching/inserting -->
				<prop key="hibernate.max_fetch_depth">3</prop>
				<prop key="hibernate.default_batch_fetch_size">${gemma.hibernate.default_batch_fetch_size}</prop>
				<prop key="hibernate.jdbc.batch_size">${gemma.hibernate.jdbc_batch_size}</prop>
				<prop key="hibernate.jdbc.batch_versioned_data">true</prop>
				<prop key="hibernate.order_inserts">true</prop>
				<prop key="hibernate.order_updates">true</prop>
				<!-- used for micrometer -->
				<prop key="hibernate.generate_statistics">true</prop>
				<!-- debugging options -->
				<prop key="hibernate.show_sql">${gemma.hibernate.show_sql}</prop>
				<prop key="hibernate.format_sql">${gemma.hibernate.format_sql}</prop>
			</props>
		</property>
	</bean>

	<!-- enable usage of @Cacheable annotations -->
	<cache:annotation-driven order="2"/>

	<!-- Spring CacheManager for @Cacheable / consumers that inject CacheManager. The "ehcache"
		 bean (a ConcurrentMapCacheManager) is provided by EhcacheConfig; alias it to "cacheManager"
		 so legacy injection points keep working. Proper EhCache 3 / Caffeine integration is a
		 follow-up phase. -->
	<alias name="ehcache" alias="cacheManager"/>
</beans>
